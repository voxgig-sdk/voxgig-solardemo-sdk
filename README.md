# Solardemo SDK

Solar System API client, generated from the OpenAPI spec.

This is an unofficial SDK for the Solar System public API, generated by Voxgig with [`@voxgig/sdkgen`](https://github.com/voxgig/sdkgen). It is not affiliated with, endorsed by, or sponsored by the upstream API provider.

Learn more about Voxgig SDKs at [voxgig.com/sdk](https://voxgig.com/sdk/).

> TypeScript, Python, PHP, Golang, Ruby, Lua, C, Clojure, C++, C#, Dart, Elixir, Java, JavaScript, Kotlin, Lean, OCaml, Perl, Python Data, Rust, Scala, Seneca Provider, Swift, Zig SDKs, a CLI with an interactive REPL, and an MCP server for AI agents — all generated from one OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).

> **Features:** `secrets`, `test` — opt-in,
> inactive until switched on, and configured per client. See the Features
> section of any SDK README below for what each one does.

## Entities, not endpoints

This SDK exposes the API as a small set of **semantic entities** — Moon and Planet — that you
call directly, instead of assembling URL paths and query strings. Entities are
**Capitalised** to mark them as the primary surface, each with the operations they
support (`list`, `load`, `create`, `update`, `remove`):

```ts
const client = new SolardemoSDK()
const items = await client.Moon().list({ planet_id: "example" })
```

Thinking in entities keeps the mental model small — for people and AI agents alike —
rather than reasoning about raw HTTP routes and query parameters.

## Offline unit testing

Every SDK ships a built-in **test mode** that swaps the HTTP transport for
an in-memory mock, so your unit tests run fully offline — no server, no
network, and no credentials:

### TypeScript

```ts
// The offline mock starts EMPTY — seed it with the records the test needs.
// Shape: { entity: { <entity-name>: { <id>: <record> } } }
const client = SolardemoSDK.test({
  entity: {
    moon: {
      test01: { id: 'test01', planet_id: 'example_planet_id', diameter: 1 },
    },
  },
})
const moons = await client.Moon().list()
// moons is an array of Moon entities, populated with mock data
// — call moons[0].data() for the record itself
console.log(moons)
```

### Python

```python
client = SolardemoSDK.test()
moons = client.Moon().list()
print(moons)
```

### PHP

```php
// Seed fixture data so offline calls resolve without a live server.
$client = SolardemoSDK::test([
    "entity" => ["moon" => ["test01" => ["id" => "test01"]]],
]);
$moons = $client->Moon()->list();
```

### Golang

```go
client := sdk.Test()
result, err := client.Moon(nil).List(
    nil, nil,
)
```

### Ruby

```ruby
# Seed fixture data so offline calls resolve without a live server.
client = SolardemoSDK.test({
  "entity" => { "moon" => { "test01" => { "id" => "test01" } } },
})
moons = client.Moon.list()
```

### Lua

```lua
local client = sdk.test()
local results, err = client:Moon():list()
```

### C

```c
#include "core/api.h"

SolardemoSDK* client = test_sdk(NULL, NULL);
PNError* err = NULL;
Entity* moon = solardemo_moon(client, NULL);
voxgig_value* moons = moon->vt->list(moon, NULL, NULL, &err);
printf("%s\n", voxgig_to_json(moons));
```

### Clojure

```clojure
(require '[sdk.api :as api]
         '[sdk.entity.moon :as e-moon]
         '[voxgig.struct :as vs])

(def client (api/test-sdk nil nil))
(def moons (e-moon/list (api/moon client nil) nil nil))
(println moons)
```

### C++

```cpp
auto client = SolardemoSDK::testSDK();
Value moons = client->moon()->list(Value::undef(), Value::undef());
std::cout << Struct::jsonify(moons) << std::endl;
```

### C#

```csharp
var client = SolardemoSDK.TestSDK(null, null);
var moonList = client.Moon().List(null);
Console.WriteLine(moonList);
```

### Dart

```dart
import 'package:solardemo_sdk/SolardemoSDK.dart';

Future<void> main() async {
  final client = SolardemoSDK.test();
  final moons = await client.Moon().list();
  print(moons);
}
```

### Elixir

```elixir
alias Solardemo.Helpers, as: H

sdk = Solardemo.test()
moon = Solardemo.moon(sdk)
records = Solardemo.Entity.Moon.list(moon, H.deep(%{}))
IO.inspect(records)
```

### Java

```java
SolardemoSDK client = SolardemoSDK.testSDK(null, null);
Object moonList = client.moon(null).list(null, null);
System.out.println(moonList);
```

### JavaScript

```js
const client = SolardemoSDK.test()
const moons = await client.Moon().list()
// moons is an array of entities, populated with mock data
// — call moons[0].data() for the record itself
console.log(moons)
```

### Kotlin

```kotlin
val client = SolardemoSDK.testSDK(null, null)
val moonList = client.moon(null).list(null, null)
println(moonList)
```

### Lean

```bash
cd lean
lake build
lake exe primary        # shared corpus: request-shaping utilities
lake exe feature        # the feature catalog (retry, cache, rbac, netsim, …)
lake exe structcorpus   # shared corpus: the vendored struct model
lake exe runner         # entity behaviour (offline; add SDK_TEST_BASE for live)
```


### OCaml

```ocaml
let () =
  let client = Sdk_client.test () in
  let results = (Sdk_client.moon client Noval).e_list (empty_map ()) Noval in
  List.iter (fun e -> print_endline (stringify (e.e_data_get ()))) results
```

### Perl

```perl
use lib 'perl/lib';
use SolardemoSDK;

my $client = SolardemoSDK->test(undef, undef);
my $moons = $client->Moon->list();
print scalar(@$moons), " records\n";
```

### Rust

```rust
use solardemo_sdk::{jo, test_sdk, Value};

let client = test_sdk(Value::Noval, Value::Noval);
let moons = client.moon(Value::Noval).list(Value::Noval, Value::Noval).unwrap();
println!("{:?}", moons);
```

### Scala

```scala
val client = SolardemoSDK.testSDK(null, null)
val moonList = client.moon(null).list(null, null)
println(moonList)
```

### Swift

```swift
let client = SolardemoSDK.testSDK(nil, nil)
let moonList = try client.Moon().list(nil, nil)
print(moonList)
```

### Zig

```zig
const std = @import("std");
const sdk = @import("sdk");
const h = sdk.h;

const client = sdk.test_sdk(h.vnull(), h.vnull());
switch (client.moon(h.vnull()).list(h.vnull(), h.vnull())) {
    .ok => |moons| std.debug.print("{s}\n", .{h.stringify(moons)}),
    .err => |e| std.debug.print("list failed: {s}\n", .{e.msg}),
}
```

## Packages

| Language | Package | Install |
| --- | --- | --- |
| TypeScript | `@voxgig-sdk/solardemo` | publish pending — [install from git tag](https://github.com/voxgig-sdk/solardemo-sdk/releases) |
| Python | `voxgig-sdk-solardemo` | publish pending — [install from git tag](https://github.com/voxgig-sdk/solardemo-sdk/releases) |
| PHP | `voxgig-sdk/solardemo` | publish pending — [install from git tag](https://github.com/voxgig-sdk/solardemo-sdk/releases) |
| Golang | `github.com/voxgig-sdk/solardemo-sdk/go` | `go get github.com/voxgig-sdk/solardemo-sdk/go@latest` |
| Ruby | `voxgig-sdk-solardemo` | publish pending — [install from git tag](https://github.com/voxgig-sdk/solardemo-sdk/releases) |
| Lua | `voxgig-sdk-solardemo` | publish pending — [install from git tag](https://github.com/voxgig-sdk/solardemo-sdk/releases) |
| C | `voxgig-sdk-solardemo` | publish pending — [install from git tag](https://github.com/voxgig-sdk/solardemo-sdk/releases) |
| Clojure | `voxgig-sdk-solardemo` | publish pending — [install from git tag](https://github.com/voxgig-sdk/solardemo-sdk/releases) |
| C++ | `voxgig-sdk-solardemo` | publish pending — [install from git tag](https://github.com/voxgig-sdk/solardemo-sdk/releases) |
| C# | `voxgig-sdk-solardemo` | publish pending — [install from git tag](https://github.com/voxgig-sdk/solardemo-sdk/releases) |
| Dart | `voxgig-sdk-solardemo` | publish pending — [install from git tag](https://github.com/voxgig-sdk/solardemo-sdk/releases) |
| Elixir | `voxgig-sdk-solardemo` | publish pending — [install from git tag](https://github.com/voxgig-sdk/solardemo-sdk/releases) |
| Java | `voxgig-sdk-solardemo` | publish pending — [install from git tag](https://github.com/voxgig-sdk/solardemo-sdk/releases) |
| JavaScript | `@voxgig-sdk/solardemo-js` | publish pending — [install from git tag](https://github.com/voxgig-sdk/solardemo-sdk/releases) |
| Kotlin | `voxgig-sdk-solardemo` | publish pending — [install from git tag](https://github.com/voxgig-sdk/solardemo-sdk/releases) |
| Lean | `voxgig-sdk-solardemo` | publish pending — [install from git tag](https://github.com/voxgig-sdk/solardemo-sdk/releases) |
| OCaml | `voxgig-sdk-solardemo` | publish pending — [install from git tag](https://github.com/voxgig-sdk/solardemo-sdk/releases) |
| Perl | `voxgig-sdk-solardemo` | publish pending — [install from git tag](https://github.com/voxgig-sdk/solardemo-sdk/releases) |
| Python Data | `voxgig-sdk-solardemo-data` | publish pending — [install from git tag](https://github.com/voxgig-sdk/solardemo-sdk/releases) |
| Rust | `voxgig-sdk-solardemo` | publish pending — [install from git tag](https://github.com/voxgig-sdk/solardemo-sdk/releases) |
| Scala | `voxgig-sdk-solardemo` | publish pending — [install from git tag](https://github.com/voxgig-sdk/solardemo-sdk/releases) |
| Seneca Provider | `voxgig-sdk-solardemo` | publish pending — [install from git tag](https://github.com/voxgig-sdk/solardemo-sdk/releases) |
| Swift | `voxgig-sdk-solardemo` | publish pending — [install from git tag](https://github.com/voxgig-sdk/solardemo-sdk/releases) |
| Zig | `voxgig-sdk-solardemo` | publish pending — [install from git tag](https://github.com/voxgig-sdk/solardemo-sdk/releases) |
| Go CLI | `github.com/voxgig-sdk/solardemo-sdk/go-cli` | `go install github.com/voxgig-sdk/solardemo-sdk/go-cli/cmd/solardemo@latest` |
| Go MCP server | `github.com/voxgig-sdk/solardemo-sdk/go-mcp` | `go get github.com/voxgig-sdk/solardemo-sdk/go-mcp@latest` |

## Quickstart

### TypeScript

```ts
import { SolardemoSDK } from '@voxgig-sdk/solardemo'

const client = new SolardemoSDK()

// List all moons (returns MoonEntity[] — .data() for the record)
const moons = await client.Moon().list({ planet_id: "example" })
for (const moon of moons) {
  console.log(moon)
}

// Load a specific moon (returns a Moon)
const moon = await client.Moon().load({
  planet_id: 'example_planet_id',
  id: 'example_id',
})
console.log(moon)
```

See the [TypeScript README](ts/README.md) for the full guide.

## Surfaces

| Surface | Path |
| --- | --- |
| **SDK** (TypeScript, Python, PHP, Golang, Ruby, Lua, C, Clojure, C++, C#, Dart, Elixir, Java, JavaScript, Kotlin, Lean, OCaml, Perl, Python Data, Rust, Scala, Seneca Provider, Swift, Zig) | `ts/` `py/` `php/` `go/` `rb/` `lua/` `c/` `clojure/` `cpp/` `csharp/` `dart/` `elixir/` `java/` `js/` `kotlin/` `lean/` `ocaml/` `perl/` `py-data/` `rust/` `scala/` `seneca-provider/` `swift/` `zig/` |
| **CLI** | `go-cli/` |
| **MCP server** | `go-mcp/` |

## Use it from an AI agent (MCP)

The generated MCP server exposes every operation in this SDK as an
[MCP](https://modelcontextprotocol.io) tool that Claude, Cursor or Cline
can call directly. Build and register it:

```bash
cd go-mcp && go build -o solardemo-mcp .
```

Then add it to your agent's MCP config (Claude Desktop, Cursor, etc.):

```json
{
  "mcpServers": {
    "solardemo": {
      "command": "/abs/path/to/solardemo-mcp"
    }
  }
}
```

## Entities

The API exposes 2 entities:

| Entity | Description | API path |
| --- | --- | --- |
| **Moon** | The Moon entity (create, list, load, remove, update). | `/api/planet/{planet_id}/moon` |
| **Planet** | The Planet entity (create, list, load, remove, update). | `/api/planet` |

The operations available across these entities are **load**, **list**, **create**, **update**, **remove** — see each entity's
own list above for exactly which it supports.

## Quickstart in other languages

### Python

```python
from solardemo_sdk import SolardemoSDK

client = SolardemoSDK()

# List all moons (returns a list, raises on error)
moons = client.Moon().list({"planet_id": "example"})
for moon in moons:
    print(moon)

# Load a specific moon (returns the record, raises on error)
moon = client.Moon().load({"id": "example_id", "planet_id": "example_planet_id"})
print(moon)
```

### PHP

```php
<?php
require_once 'solardemo_sdk.php';

$client = new SolardemoSDK();

// List all moons (returns an array; throws on error)
$moons = $client->Moon()->list();
print_r($moons);

// Load a specific moon (returns the ENTITY; call data_get() for the record; throws on error)
$moon = $client->Moon()->load(["id" => "example_id", "planet_id" => "example_planet_id"]);
print_r($moon);
```

### Golang

```go
import sdk "github.com/voxgig-sdk/solardemo-sdk/go"

client := sdk.New()

// List all moons
moons, err := client.Moon(nil).List(nil, nil)
if err != nil {
    panic(err)
}
fmt.Println(moons)

// Load a specific moon
moon, err := client.Moon(nil).Load(
    map[string]any{"planet_id": "example_planet_id", "id": "example_id"}, nil,
)
if err != nil {
    panic(err)
}
fmt.Println(moon)
```

### Ruby

```ruby
require_relative "Solardemo_sdk"

client = SolardemoSDK.new

# List all moons (returns an Array; raises on error)
moons = client.Moon.list
puts moons

# Load a specific moon (returns the ENTITY; call data_get for the record)
moon = client.Moon.load({ "id" => "example_id", "planet_id" => "example_planet_id" })
puts moon
```

### Lua

```lua
local sdk = require("solardemo_sdk")

local client = sdk.new()

-- List all moons
local moons, err = client:Moon():list()
print(moons)

-- Load a specific moon
local moon, err = client:Moon():load({ id = "example_id", planet_id = "example_planet_id" })
print(moon)
```

### C

```c
#include "core/api.h"

SolardemoSDK* client = solardemo_sdk_new(NULL);
PNError* err = NULL;

Entity* moon = solardemo_moon(client, NULL);

// List all moons (returns a List, sets *err on failure)
voxgig_value* moons = moon->vt->list(moon, NULL, NULL, &err);
for (size_t i = 0; i < (size_t)voxgig_size(moons); i++) {
    printf("%s\n", voxgig_to_json(voxgig_getelem(moons, v_int(i), NULL)));
}

// Load a specific moon (returns the record, sets *err on failure)
voxgig_value* moon_rec = moon->vt->load(moon, cmap(2, "id", v_str("example_id"), "planet_id", v_str("example_planet_id")), NULL, &err);
printf("%s\n", voxgig_to_json(moon_rec));
```

### Clojure

```clojure
(require '[sdk.api :as api]
         '[sdk.entity.moon :as e-moon]
         '[voxgig.struct :as vs])

(def client (api/make-sdk nil))

;; List all moons (returns a vector, raises on error)
(doseq [moon (e-moon/list (api/moon client nil) nil nil)]
  (println moon))

;; Load a specific moon (returns the record, raises on error)
(def moon (e-moon/load (api/moon client nil) (vs/jm "id" "example_id" "planet_id" "example_planet_id") nil))
(println moon)
```

### C++

```cpp
#include "core/sdk.hpp"

using namespace sdk;

auto client = SolardemoSDK::create();

// List all moons (returns a Value list, throws on error)
Value moons = client->moon()->list(Value::undef(), Value::undef());
for (const auto& moon : *moons.as_list()) {
  std::cout << Struct::jsonify(moon) << std::endl;
}

// Load a specific moon (returns the record, throws on error)
Value moon = client->moon()->load(vmap({{"id", Value("example_id")}, {"planet_id", Value("example_planet_id")}}), Value::undef());
std::cout << Struct::jsonify(moon) << std::endl;
```

### C#

```csharp
using SolardemoSdk;

var client = new SolardemoSDK();

// List all moons (returns object?, an aggregate list; raises on error)
var moonList = client.Moon().List(null);
Console.WriteLine(moonList);

// Load a specific moon (returns the record, raises on error)
var moon = client.Moon().Load(new Dictionary<string, object?> { ["id"] = "example_id", ["planet_id"] = "example_planet_id" });
Console.WriteLine(moon);
```

### Dart

```dart
import 'package:solardemo_sdk/SolardemoSDK.dart';

Future<void> main() async {
  final client = SolardemoSDK();

  // List all moons (returns a list of entities, throws on error)
  final moons = await client.Moon().list({ planet_id: "example" });
  for (final item in moons) {
    print(item.data());
  }

  // Load a specific moon (returns the record, throws on error)
  final moon = await client.Moon().load({'id': 'example_id', 'planet_id': 'example_planet_id'});
  print(moon);
}
```

### Elixir

```elixir
alias Solardemo.Helpers, as: H

sdk = Solardemo.new()

moon = Solardemo.moon(sdk)

# List all moon records (raises on error)
records = Solardemo.Entity.Moon.list(moon)
IO.inspect(records)

# Load a specific moon (returns the record, raises on error)
record = Solardemo.Entity.Moon.load(moon, H.deep(%{"id" => "example_id", "planet_id" => "example_planet_id"}))
IO.inspect(record)
```

### Java

```java
import voxgig.solardemosdk.core.SolardemoSDK;

SolardemoSDK client = new SolardemoSDK();

// List all moons (returns Object, an aggregate list; raises on error)
Object moonList = client.moon(null).list(null, null);
System.out.println(moonList);

// Load a specific moon (returns the record, raises on error)
Object moon = client.moon(null).load(Map.of("id", "example_id", "planet_id", "example_planet_id"), null);
System.out.println(moon);
```

### JavaScript

```js
const { SolardemoSDK } = require('@voxgig-sdk/solardemo-js')

const client = new SolardemoSDK()

// List all moons (returns an array)
const moons = await client.Moon().list({ planet_id: "example" })
for (const moon of moons) {
  console.log(moon)
}

// Load a specific moon (returns the entity)
const moon = await client.Moon().load({
  planet_id: 'example_planet_id',
  id: 'example_id',
})
console.log(moon)
```

### Kotlin

```kotlin
import voxgig.solardemosdk.core.SolardemoSDK

val client = SolardemoSDK()

// List all moons (returns Any?, an aggregate list; raises on error)
val moonList = client.moon(null).list(null, null)
println(moonList)

// Load a specific moon (returns the record, raises on error)
val moon = client.moon(null).load(mutableMapOf<String, Any?>("id" to "example_id", "planet_id" to "example_planet_id"), null)
println(moon)
```

### Lean

```lean
import SdkClient
open VoxgigStruct

def main : IO Unit := do
  let ctx ← mkCtx
  (do
    let sdk ← Sdk.newSdk (← emptyMap)
    IO.println s!"client ready"
  ).run ctx
```

Build it with `cd lean && lake build`.


### OCaml

```ocaml
open Voxgig_struct
open Sdk_helpers

let () =
  let client = Sdk_client.make0 () in
  (* List all moon records (one ENTITY per record; raises on error) *)
  let moons = (Sdk_client.moon client Noval).e_list (empty_map ()) Noval in
  List.iter (fun e -> print_endline (stringify (e.e_data_get ()))) moons;
  (* Load a specific moon (returns the ENTITY; raises on error) *)
  let moon = (Sdk_client.moon client Noval).e_load (jo [("id", (Str "example_id")); ("planet_id", (Str "example_planet_id"))]) Noval in
  print_endline (stringify (moon.e_data_get ()))
```

### Perl

```perl
use lib 'perl/lib';
use SolardemoSDK;

my $client = SolardemoSDK->new;

# List all moons (returns an arrayref; dies on error)
my $moons = $client->Moon->list;
for my $moon (@$moons) {
    print "$moon->{id}\n";
}

# Load a specific moon (returns the ENTITY; call data_get for the record; dies on error)
my $moon = $client->Moon->load({ 'id' => 'example_id', 'planet_id' => 'example_planet_id' });
print "$moon->{id}\n";
```

### Rust

```rust
use solardemo_sdk::{jo, SolardemoSDK, Value};

let client = SolardemoSDK::new(Value::Noval);

// List all moons (returns a Value::List, Err on failure)
let moons = client.moon(Value::Noval).list(Value::Noval, Value::Noval).unwrap();
if let Value::List(items) = &moons {
    for moon in items.borrow().iter() {
        println!("{:?}", moon);
    }
}

// Load a specific moon (returns the record, Err on failure)
let moon = client.moon(Value::Noval).load(jo(vec![("id", Value::str("example_id")), ("planet_id", Value::str("example_planet_id"))]), Value::Noval).unwrap();
println!("{:?}", moon);
```

### Scala

```scala
import voxgig.solardemosdk.core.SolardemoSDK

val client = new SolardemoSDK()

// List all moons (returns Object, an aggregate list; raises on error)
val moonList = client.moon(null).list(null, null)
println(moonList)

// Load a specific moon (returns the record, raises on error)
val moon = client.moon(null).load(java.util.Map.of("id", "example_id", "planet_id", "example_planet_id"), null)
println(moon)
```

### Swift

```swift
import SolardemoSdk

let client = SolardemoSDK()

// List all moons (returns a Value list, throws on error)
let moonList = try client.Moon().list(nil, nil)
for moon in moonList.asList?.items ?? [] {
    print(moon)
}

// Load a specific moon (returns the record, throws on error)
let moon = try client.Moon().load(VMap([("id", .string("example_id")), ("planet_id", .string("example_planet_id"))]), nil)
print(moon)
```

### Zig

```zig
const std = @import("std");
const sdk = @import("sdk");
const h = sdk.h;

const client = sdk.SolardemoSDK.new(h.vnull());

// List all moons (Ok is a Value array, .err on failure)
switch (client.moon(h.vnull()).list(h.vnull(), h.vnull())) {
    .ok => |moons| std.debug.print("{s}\n", .{h.stringify(moons)}),
    .err => |e| std.debug.print("list failed: {s}\n", .{e.msg}),
}

// Load a specific moon (Ok is the record, .err on failure)
switch (client.moon(h.vnull()).load(h.jo(&.{.{ "id", h.vstr("example_id") }, .{ "planet_id", h.vstr("example_planet_id") }}), h.vnull())) {
    .ok => |moon| std.debug.print("{s}\n", .{h.stringify(moon)}),
    .err => |e| std.debug.print("load failed: {s}\n", .{e.msg}),
}
```

## Direct and prepare

For endpoints the entity model doesn't cover, use the low-level methods:

- **`direct(fetchargs)`** — build and send an HTTP request in one step.
- **`prepare(fetchargs)`** — build the request without sending it.

Both accept a map with `path`, `method`, `params`, `query`,
`headers`, and `body`. See the [How-to guides](#how-to-guides) below.

## How-to guides

### Make a direct API call

When the entity interface does not cover an endpoint, use `direct`:

**TypeScript:**
```ts
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

**Python:**
```python
result = client.direct({
    "path": "/api/resource/{id}",
    "method": "GET",
    "params": {"id": "example"},
})
```

**PHP:**
```php
$result = $client->direct([
    "path" => "/api/resource/{id}",
    "method" => "GET",
    "params" => ["id" => "example"],
]);
```

**Go:**
```go
result, err := client.Direct(map[string]any{
    "path":   "/api/resource/{id}",
    "method": "GET",
    "params": map[string]any{"id": "example"},
})
if err != nil {
    panic(err)
}
fmt.Println(result)
```

**Ruby:**
```ruby
result = client.direct({
  "path" => "/api/resource/{id}",
  "method" => "GET",
  "params" => { "id" => "example" },
})
```

**Lua:**
```lua
local result, err = client:direct({
  path = "/api/resource/{id}",
  method = "GET",
  params = { id = "example" },
})
```

**C:**
```c
PNError* err = NULL;
voxgig_value* result = sdk_direct(client, cmap(3,
    "path", v_str("/api/resource/{id}"),
    "method", v_str("GET"),
    "params", cmap(1, "id", v_str("example"))), &err);
```

**Clojure:**
```clojure
(def result
  (api/direct client
    (vs/jm "path" "/api/resource/{id}"
           "method" "GET"
           "params" (vs/jm "id" "example"))))
```

**C++:**
```cpp
Value result = client->direct(vmap({
    {"path", Value("/api/resource/{id}")},
    {"method", Value("GET")},
    {"params", vmap({{"id", Value("example")}})},
}));
```

**C#:**
```csharp
var result = client.Direct(new Dictionary<string, object?>
{
    ["path"] = "/api/resource/{id}",
    ["method"] = "GET",
    ["params"] = new Dictionary<string, object?> { ["id"] = "example" },
});
```

**Dart:**
```dart
final result = await client.direct({
  'path': '/api/resource/{id}',
  'method': 'GET',
  'params': {'id': 'example'},
});
```

**Elixir:**
```elixir
result = Solardemo.direct(sdk, Solardemo.Helpers.deep(%{
  "path" => "/api/resource/{id}",
  "method" => "GET",
  "params" => %{"id" => "example"}
}))
```

**Java:**
```java
Map<String, Object> result = client.direct(Map.of(
    "path", "/api/resource/{id}",
    "method", "GET",
    "params", Map.of("id", "example")));
```

**JavaScript:**
```js
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

**Kotlin:**
```kotlin
val result = client.direct(mutableMapOf<String, Any?>(
    "path" to "/api/resource/{id}",
    "method" to "GET",
    "params" to mapOf("id" to "example")))
```

For Lean, point the client at another server with the `base` option,
and run the entity suite against it with `SDK_TEST_BASE`:

```bash
cd lean && SDK_TEST_BASE=http://localhost:8901 lake exe runner
```

**OCaml:**
```ocaml
let result = Sdk_client.direct client (jo [
    ("path", Str "/api/resource/{id}");
    ("method", Str "GET");
    ("params", jo [("id", Str "example")]);
]) in
ignore result
```

**Perl:**
```perl
my $result = $client->direct({
    'path' => '/api/resource/{id}',
    'method' => 'GET',
    'params' => { 'id' => 'example' },
});
```

**Rust:**
```rust
let result = client.direct(jo(vec![
    ("path", Value::str("/api/resource/{id}")),
    ("method", Value::str("GET")),
    ("params", jo(vec![("id", Value::str("example"))])),
]));
```

**Scala:**
```scala
val result = client.direct(java.util.Map.of(
    "path", "/api/resource/{id}",
    "method", "GET",
    "params", java.util.Map.of("id", "example")))
```

**Swift:**
```swift
let result = client.direct(VMap([
    ("path", .string("/api/resource/{id}")),
    ("method", .string("GET")),
    ("params", .map([("id", .string("example"))])),
]))
```

**Zig:**
```zig
const result = client.direct(h.jo(&.{
    .{ "path", h.vstr("/api/resource/{id}") },
    .{ "method", h.vstr("GET") },
    .{ "params", h.jo(&.{.{ "id", h.vstr("example") }}) },
}));
```

## Advanced

> Everyday use only needs the sections above. This explains the internals
> behind every call — relevant when writing custom features.

Every SDK call runs the same five-stage pipeline:

1. **Point** — resolve the API endpoint from the operation definition.
2. **Spec** — build the HTTP specification (URL, method, headers, body).
3. **Request** — send the HTTP request.
4. **Response** — receive and parse the response.
5. **Result** — extract the result data for the caller.

A feature hook fires at each stage (e.g. `PrePoint`, `PreSpec`,
`PreRequest`), so features can inspect or modify the pipeline without
forking the SDK.

### Features

| Feature | Purpose |
| --- | --- |
| **SecretsFeature** | Secret access: resolve the API credential through a provider chain, and exchange a refresh token for short-lived access tokens |
| **TestFeature** | In-memory mock transport for testing without a live server |

Pass custom features via the `extend` option at construction time.

## Customizing this SDK

This repository contains its own generator (`.sdk/`), so the SDK is
customizable without forking any upstream tool:

- **The model** (`.sdk/model/`) declares everything this project owns:
  package names, versions, active features, per-target settings. It is
  written in [aontu](https://aontu.dev), a JSON-based
  specification language designed for building ontologies: easy to edit
  by hand, and files unify rather than override, so small declarations
  compose into one model. Regeneration re-reads it every time.
- **Templates** (`.sdk/tm/`) and **components** (`.sdk/src/cmp/`) are
  the two layers of generation, copied into this repo: templates are the
  literal per-language source, components generate the API-shaped parts.
- **Regeneration merges.** By default, newly generated content is
  three-way merged into existing files, so generator updates and local
  edits usually converge without manual conflict handling. A project can
  opt for plain overwrite instead.
- **Custom features and entire custom targets** arrive through sdkgen
  packages (`voxgig-sdkgen package add`), on the same rails as the
  bundled languages, and `voxgig-sdkgen doctor` reports any drift from
  what a resync would write.

How-to: [customize and propagate templates](https://github.com/voxgig/sdkgen/blob/main/docs/how-to/customize-and-propagate-templates.md).
The full story: [voxgig.com/sdk/custom](https://voxgig.com/sdk/custom).

## Per-language documentation

- [TypeScript](ts/README.md)
- [Python](py/README.md)
- [PHP](php/README.md)
- [Golang](go/README.md)
- [Ruby](rb/README.md)
- [Lua](lua/README.md)
- [C](c/README.md)
- [Clojure](clojure/README.md)
- [C++](cpp/README.md)
- [C#](csharp/README.md)
- [Dart](dart/README.md)
- [Elixir](elixir/README.md)
- [Java](java/README.md)
- [JavaScript](js/README.md)
- [Kotlin](kotlin/README.md)
- [Lean](lean/README.md)
- [OCaml](ocaml/README.md)
- [Perl](perl/README.md)
- [Python Data](py-data/README.md)
- [Rust](rust/README.md)
- [Scala](scala/README.md)
- [Seneca Provider](seneca-provider/README.md)
- [Swift](swift/README.md)
- [Zig](zig/README.md)

## Upstream API

This SDK is generated from the upstream OpenAPI specification. It is an
unofficial client and is not affiliated with the API provider.

The OpenAPI spec(s) this SDK was generated from are kept in the
[`.sdk/def/`](.sdk/def/) folder.

- Upstream API: [http://localhost:8901](http://localhost:8901)

## Security

Please report security issues to security@voxgig.com. See [SECURITY.md](SECURITY.md).
Do not open public issues for suspected vulnerabilities.

---

Generated from the Solar System API OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).
